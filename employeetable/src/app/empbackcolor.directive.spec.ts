import { EmpbackcolorDirective } from './empbackcolor.directive';

describe('EmpbackcolorDirective', () => {
  it('should create an instance', () => {
    const directive = new EmpbackcolorDirective();
    expect(directive).toBeTruthy();
  });
});
